/** Public confirmation route — never append internal lead UUIDs. */
export const PROJECT_REQUEST_RECEIVED_PATH = "/project-request-received";

export function getProjectRequestReceivedPath() {
  return PROJECT_REQUEST_RECEIVED_PATH;
}
